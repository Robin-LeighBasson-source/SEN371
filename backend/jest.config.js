module.exports = {
  testEnvironment: "node",
  testTimeout: 30000,
  // Existing unit tests live in test/; integration tests in tests/
  testMatch: ["**/test/**/*.test.js", "**/tests/**/*.test.js"],
};
